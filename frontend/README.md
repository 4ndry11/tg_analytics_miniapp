# Analytics Mini App - Frontend

Telegram Mini App для аналітики лідів та продажів.

## Швидкий старт

### 1. Встановлення залежностей

```bash
npm install
```

### 2. Налаштування Environment Variables

Створіть файл `.env` на основі `.env.example`:

```bash
cp .env.example .env
```

Відредагуйте `.env` та встановіть URL вашого backend API:

```
VITE_API_URL=https://your-backend-api.onrender.com
```

### 3. Запуск в режимі розробки

```bash
npm run dev
```

Додаток буде доступний за адресою `http://localhost:5173`

### 4. Білд для production

```bash
npm run build
```

Файли для деплоя будуть створені в директорії `dist/`

## Деплой на Render

1. Завантажте проект на GitHub
2. Створіть новий Static Site на Render
3. Вкажіть Build Command: `npm run build`
4. Вкажіть Publish Directory: `dist`
5. Додайте Environment Variable: `VITE_API_URL=https://your-backend-api.onrender.com`

## Структура проекту

```
src/
├── components/        # Переиспользуемые компоненты
│   ├── MetricCard/
│   ├── AlertBanner/
│   └── InteractiveChart/
├── pages/            # Страницы приложения
│   ├── Home.tsx      # Главная страница
│   ├── Leads.tsx     # Аналитика лидов
│   └── Sales.tsx     # Аналитика продаж
├── services/         # API сервисы
│   └── api.ts
├── types/            # TypeScript типы
│   └── index.ts
├── styles/           # Глобальные стили
│   └── global.css
├── App.tsx           # Главный компонент
└── main.tsx          # Точка входа
```

## Функционал

- 📊 Интерактивные дашборды с графиками
- 🔍 Drill-down аналитика по менеджерам
- ⚠️ Система алертов
- 📅 Выбор периода (день/неделя/месяц)
- 💰 Интеграция с Finmap
- 📱 Адаптивный дизайн для мобильных устройств
